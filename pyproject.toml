[project]
name = "horizon-product-intelligence"
version = "0.1.0"
description = "AI-driven product opportunity intelligence engine"
authors = [
    { name = "Jeremy", email = "your-email@example.com" }
]
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "streamlit>=1.32",
    "pydantic>=2.6",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
